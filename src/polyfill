// This file provides polyfills for various environment variables and globals

// Polyfill for process.env
if (typeof window !== 'undefined' && typeof window.process === 'undefined') {
  window.process = {
    env: {
      NODE_ENV: 'production'
    }
  };
}

// Add other polyfills as needed